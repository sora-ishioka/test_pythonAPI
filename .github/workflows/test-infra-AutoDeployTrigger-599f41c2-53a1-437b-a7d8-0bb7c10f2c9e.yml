name: Network Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: DNS Check
        run: |
          nslookup rddx-func-user4.azurewebsites.net
          nslookup rddx-func-user4.scm.azurewebsites.net

      - name: Connectivity Check
        run: |
          curl -Iv https://rddx-func-user4.scm.azurewebsites.net
